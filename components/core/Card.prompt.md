Base surface container for grouping content — content tiles, stat blocks, list rows.

```jsx
<Card padding="20px"><Badge>Categoría 4</Badge></Card>
```

Sits one step lighter than page background (`--bg-surface`), 18px radius, subtle border, soft drop shadow.
